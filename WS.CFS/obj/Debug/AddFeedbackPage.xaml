<Page
    x:Name="pageRoot"
    x:Class="WS.CFS.AddFeedbackPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WS.CFS"
    xmlns:common="using:WS.CFS.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="White" RequestedTheme="Light" RenderTransformOrigin="0.501,0.488">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid Background="#FF2C71B9" RequestedTheme="Dark">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,57,0,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="New Feedback" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,44" Foreground="White"/>
        </Grid>
        <TextBox HorizontalAlignment="Center" Margin="480,98,486,0" Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Top" Width="400" Height="42" FontSize="20" Padding="10,5" MinHeight="42" BorderBrush="#FFDDDDDD"/>
        <TextBox HorizontalAlignment="Left" Margin="480,339,0,0" Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Top" RenderTransformOrigin="0.493,0.503" Width="400" Height="182" Padding="10,5" MinHeight="42" AcceptsReturn="True" BorderBrush="#FFDDDDDD"/>
        <ComboBox HorizontalAlignment="Left" Margin="480,216,0,0" Grid.Row="1" VerticalAlignment="Top" Width="400" Height="42" MinHeight="42" Padding="10,5" BorderBrush="#FFDDDDDD">
            <ComboBoxItem Content="Item 1" FontSize="20"/>
        </ComboBox>
        <TextBlock HorizontalAlignment="Left" Margin="480,43,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Title" VerticalAlignment="Top" FontSize="42" Foreground="#FF2C71B9"/>
        <TextBlock HorizontalAlignment="Left" Margin="480,161,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Type" VerticalAlignment="Top" FontSize="42" Foreground="#FF2C71B9"/>
        <TextBlock HorizontalAlignment="Left" Margin="480,284,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Description" VerticalAlignment="Top" FontSize="42" Foreground="#FF2C71B9"/>
    </Grid>
</Page>

